{
  "name": "liff-app",
  "version": "1.0.0",
  "description": "LINE Things Starter 向け LIFF アプリのサンプルコードです。 初期状態の Starter ファームウェアで動作する LIFF アプリのコードが含まれています。",
  "main": "liff.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build index.html",
    "start": "npm run build && node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.18.0",
    "connect-history-api-fallback": "^1.6.0",
    "dotenv": "^7.0.0",
    "express": "^4.16.4",
    "parcel-bundler": "^1.12.2"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.2"
  }
}
